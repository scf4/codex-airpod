{
  "name": "codex-airpod",
  "version": "0.1.1",
  "private": true,
  "description": "Use AirPods to mute and unmute Codex Voice in ChatGPT for macOS",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "launch": "node src/launch.cjs",
    "check": "set -e; for file in src/*.cjs test/*.cjs; do node --check \"$file\"; done",
    "test": "node --test test/*.test.cjs"
  }
}
